# pnpm 10 will not run a dependency's install scripts unless it is listed here, and
# `ng build` fails its deps check while any are outstanding.
allowBuilds:
  "@parcel/watcher": true
  esbuild: true
  lmdb: true
  msgpackr-extract: true
